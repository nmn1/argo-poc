apiVersion: v1
kind: Service
metadata:
  name: bg-service
  namespace: app-bg
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /index.html
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: BG_APP
